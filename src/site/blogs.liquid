---
title: Blog
layout: main
pagination:
    data: directus_blogs
    size: 1
    alias: blog
permalink: "blog/{{ blog.slug }}/"
---
<h1>{{ blog.title }}</h1>
<img src="{{ blog.hero_image.data.full_url }}" alt="{{ blog.hero_image.title }}">
<div>{{ blog.body }}</div>